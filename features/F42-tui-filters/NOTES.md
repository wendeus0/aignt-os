# F42 Notes

- A feature ficou restrita a filtragem visual client-side no dashboard TUI atual de `aignt runs watch`.
- O baseline entregue usa as teclas `f` para falhas, `r` para atividade e `x` para restaurar todos os steps.
- O recorte nao adiciona busca textual, persistencia de filtro entre sessoes nem combinacao complexa de filtros.
- A feature opera apenas sobre a visualizacao local do AIgnt-Synapse-Flow, a engine propria de pipeline do AIgnt OS.
