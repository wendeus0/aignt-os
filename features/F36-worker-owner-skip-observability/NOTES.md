# F36 Notes

- O evento deve ser gravado na run incompatível, nao na run compativel processada.
- A deduplicacao vale apenas para o mesmo motivo de skip consecutivo; se a mensagem mudar, um novo evento e aceitavel.
- `runs show` ja consegue exibir o novo evento via fluxo atual; nao ha necessidade de painel ou subcomando novo.
